import { SingletonRedirect } from './SingletonRedirect'

const SlideshowSettingsList = () =>
  <SingletonRedirect collectionSlug="slideshow-settings-collection" />

export default SlideshowSettingsList
